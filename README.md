# COMP3610_A1

To setup evironment run the following in terminal 

    pip install -r requirements.txt

StreamLit link

https://dscomp3610a1.streamlit.app/

To run StreamLit app locally, run the following command in terminal

    streamlit run {file_path}dashboard/app.py

**Must run pip install -r requirements.txt as polars is not in the streamlit library**